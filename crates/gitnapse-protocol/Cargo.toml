[package]
name = "gitnapse-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Communication protocol between the GitNapse core and any interface. Transport-agnostic types, no gitnapse dependency."

[dependencies]
serde = { workspace = true }
